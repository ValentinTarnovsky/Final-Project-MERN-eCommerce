import { Box } from "@mui/material";
import "./logo.scss";

const Logo = () => {
    return (
        <Box className="logo">
            <h3>
                trend<span>.</span>
            </h3>
        </Box>
    );
};

export default Logo;